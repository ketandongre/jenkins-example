pipeline {
    agent any
    stages {
        stage('Jenkinsfile added'){
           steps{
                step('test-step'){
                    echo '${env}'
                }
           }
        }
    }
}